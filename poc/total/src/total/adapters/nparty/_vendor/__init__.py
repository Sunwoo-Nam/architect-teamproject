"""dp2-nparty 프로토콜 원본 이식 (동작 무변경).

`poc/dp2-nparty/src/dp2_nparty/`에서 그대로 가져왔다. **재작성하지 않은 이유**:
통합의 검증 기준이 "기존 실행과 같은 수치가 나오는가"인데, 프로토콜을 다시 쓰면
차이가 이식 버그인지 재작성 차이인지 가릴 수 없다. 원본을 보존하고 계약 변환만
어댑터(`..adapter`)에서 한다.

2026-08-13 2차 이식으로 측정기(`measures/`)와 리포트 생성기까지 모두 가져왔다.
가져온 파일과 원본 대비 수정한 곳은 `README.md`에 정리돼 있다.
"""

# 1차 이식(프로토콜만)에서는 ufun_provider.py를 뺐었다 — 개발용 임시 Ufun이라
# 확정 벤치마크만 쓰면 필요 없다고 봤기 때문이다. 2차 이식에서 되돌렸다:
# harness.py·campaign.py·measures/an_kit.py가 TableUfun을 직접 임포트하므로
# 빼면 그 세 모듈이 임포트되지 않는다.
