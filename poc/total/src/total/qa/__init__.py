"""공통 QA 측정 라이브러리 — [`24-QA-측정-핸드북`](../../../../docs/changbae/24-QA-측정-핸드북.md) 정본.

`poc/dp2-nparty`(방안 1-A vs 방안 2)와 `poc/dp-composite-agenda`(1안 vs 2안)가
**같은 자로** 재도록 측정 코드를 한 곳에 모은 것이다. 두 실험은 서로 다른 설계 문제이므로
**각각 독립 실행**하고, 공유하는 것은 측정기와 결과 형식뿐이다.

## 다루는 QA

| 모듈 | QA | 판정 지표 | 24 |
|---|---|---|---|
| `fc` | Functional Correctness | 달성률 · 개선 비율 s (**2지표 병행**) | §1 |
| `cf` | Confidentiality | 노출 배수 m (노출률·집중도 병기) | §7 |
| `tb` | Time Behaviour | 합성 시간 + 지배 항 | §6 |
| `ru` | Resource Utilization-메모리 | 프로세스 피크 · 공통 기저 (**각각**) | §2 |
| `sc_issue` | Scalability-의제 | 탄력성 c · 최대 의제 수 (**2지표 병행**) | §4 |

## 다루지 **않는** QA와 그 이유

나중에 "왜 없지?"가 되지 않도록 남긴다.

- **Fault Tolerance · Recoverability** — 이번 범위에서 제외 (사용자 지시 2026-08-12).
  기존 두 PoC에 구현이 있으나 측정 방식이 근본적으로 다르다 (dp2는 실측 kill-resume,
  dpca는 해석 모델). 통합하려면 어느 쪽이 정본인지부터 정해야 한다.
- **Scalability-참여자 수** — dp2 전용 축이다. dpca는 참여자 2인 고정이라 잴 것이 없다.
- **Analysability · Adaptability** — 이번 범위 밖.

## 설계 원칙 두 가지

1. **측정기는 도메인을 모른다.** `contract.py`의 타입에만 의존하고, 각 실험이 어댑터로
   자기 도메인을 맞춘다. 측정기가 계약 밖 필드를 읽기 시작하면 공통 라이브러리가 아니다.
2. **별점만 내지 않는다.** 여러 밴드가 잠정이므로(24 §3.3 등) 원지표를 항상 함께 낸다.
"""
